export interface ProfileUserData {
  name: string;
  email: string;
  username: string;
  role: string;
  avatarUrl: string;
}

export interface MyProfile {
  fullName: string;
  organization: string;
  avatarUrl: string;
}
